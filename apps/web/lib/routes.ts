/**
 * Shared route targets for CTAs that must stay in sync.
 *
 * "Empezar" appears in the nav AND on the landing tagline; both point here so
 * the destination changes in exactly one place when the onboarding flow it
 * should open gets defined. /works (discover) is the provisional target.
 */
export const START_HREF = "/works";
